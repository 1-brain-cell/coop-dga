# Checklist: รายการแก้ไขและปรับปรุงบทเรียน (Day 0 - Day 5)

ตารางเช็คลิสต์ด้านล่างนี้รวบรวมบั๊กการทำงาน (Bugs), ประเด็นความเสถียรของโค้ด (Code Quality) และจุดที่สามารถปรับปรุงประสบการณ์ผู้เรียน (UI/UX) ที่ตรวจพบจากระบบบทเรียนทั้งหมด เพื่อให้แชทในรอบถัดไปสามารถนำไปอ่านประเมินและดำเนินการแก้ไขได้อย่างเป็นระบบครับ

---

## 📊 1. หน้า `day5.html` (Data Storytelling & Visualization)

- [x] **[Bug - Critical] แก้ไขการตรวจสอบพื้นที่คลิกในด่านที่ 7 (Spot the Flaw Game)**
  - *ปัญหา*: ฟังก์ชัน `checkHit` ในด่านที่ 7 (เรื่อง Overplotting) คืนค่าเป็น `true` เสมอ ทำให้ผู้เรียนกดตรงไหนก็ได้คำตอบว่าถูก
  - *จุดแก้ไข*: ฟังก์ชัน `checkHit` ใน `levels[6]` (ด่านที่ 7)
  - *แนวทาง*: เปลี่ยนจาก `(x, y) => true` เป็นการคำนวณหาระยะทางรอบจุดศูนย์กลางของ Cluster (พิกัด 355, 312 รัศมี 72 พิกเซล):
    ```javascript
    checkHit: (x, y) => Math.hypot(x - 355, y - 312) <= 72
    ```

- [x] **[Code Quality] ปรับปรุงการสุ่มพิกัด Bubble ในด่านที่ 7 ไม่ให้กะพริบขยับตำแหน่ง (Flickering)**
  - *ปัญหา*: มีการสุ่มตำแหน่ง Bubble 25 จุดด้วย `Math.random()` ภายในฟังก์ชัน `draw()` ทำให้เวลาคลิกหรือเปลี่ยนระดับหน้าจอ จุดทั้งหมดจะสุ่มพิกัดและขนาดใหม่ทันที
  - *จุดแก้ไข*: ฟังก์ชัน `draw()` และระบบการเก็บพิกัดในด่านที่ 7
  - *แนวทาง*: ย้ายโค้ดสุ่มจุดไปไว้ในขั้นตอนการเริ่มเกม (`initGame()` หรือกำหนดเป็นชุดค่าคงที่แบบ Static) แล้วให้ `draw()` ดึงมาแสดงผลตามพิกัดเดิมเสมอ

---

## 🗄️ 2. หน้า `day3.html` (Data Warehouse & ETL)

- [x] **[Code Quality] ปรับปรุงการสกัดพารามิเตอร์ของตัวเลือกคำตอบเพื่อลดความเปราะบาง (Refactor Parsing)**
  - *ปัญหา*: ฟังก์ชัน `answerViz()` และ `identifyFormat()` ใช้ Regex ไป Parse สตริงจากแอตทริบิวต์ `onclick` เพื่อหาค่าคำตอบที่ถูกต้อง ซึ่งจะพังทันทีถ้าในอนาคตมีคนเว้นวรรคช่องว่างใน HTML ใหม่
  - *จุดแก้ไข*: โครงสร้างปุ่มตัวเลือกใน HTML และฟังก์ชันประมวลผลคำตอบใน JS
  - *แนวทาง*: เปลี่ยนไปใช้ custom data attributes ของ HTML5 เช่น `data-choice` และ `data-correct` บนปุ่มตัวเลือก แล้วเรียกผ่าน `btn.dataset.choice` ใน JS แทนการ Parse ด้วย Regex

- [x] **[UI/UX] ปรับปรุงระบบลากวาง ETL Pipeline ให้ยืดหยุ่นขึ้น (Drag-and-Drop Return)**
  - *ปัญหา*: เมื่อวางการ์ดขั้นตอนลงในช่องเป้าหมายแล้ว การ์ดจะถูกล็อกทันที หากวางผิดที่ผู้เรียนจะไม่สามารถลากย้ายช่องหรือดึงการ์ดกลับได้ ต้องกดเริ่มใหม่ทั้งหมด
  - *จุดแก้ไข*: อีเวนต์และฟังก์ชันเกี่ยวกับ Drag & Drop (`dropItem()` / `resetDnD()`)
  - *แนวทาง*: เพิ่มปุ่มกากบาทเล็ก ๆ หรือเขียนให้สามารถคลิกที่ช่องเป้าหมายเพื่อลบการ์ดใบนั้นกลับคืนสู่กล่องเริ่มต้นได้โดยไม่ต้องกดเริ่มใหม่ทั้งหมด

---

## 🐍 3. หน้า `day1.html` (Python & Data Exploration)

- [x] **[Code Quality] ป้องกันข้อผิดพลาดดัชนีสีเกินขอบเขตใน K-Means (Array Index Overflow)**
  - *ปัญหา*: ตัวแปร `kmColors` เก็บสีไว้ 5 สี หากในอนาคตมีผู้ขยายแถบสไลเดอร์ K ให้เลือกได้มากกว่า 5 กลุ่ม (เกิน `kmColors.length`) โค้ดจะพ่นค่า `undefined` และสไตล์พัง
  - *จุดแก้ไข*: การกำหนดสีจุด `ctx.fillStyle` ในฟังก์ชัน `drawKMeans()`
  - *แนวทาง*: นำ Operator Modulo (`%`) มาช่วยครอบดัชนี เพื่อให้สุ่มวนใช้สีเก่าได้เมื่อกลุ่มเกินกำหนด:
    ```javascript
    ctx.fillStyle = kmColors[i % kmColors.length];
    ```

- [x] **[UI/UX] ป้องกันปัญหาช่องว่างส่วนเกินในการตรวจสอบ Key ของ Dictionary**
  - *ปัญหา*: การป้อนข้อมูล Key ใน Dictionary Playground หากพิมพ์เคาะเว้นวรรค เช่น `"name "` จะถูกบันทึกเป็นคนละคีย์กับ `"name"` และค้นหาไม่เจอ
  - *จุดแก้ไข*: ฟังก์ชัน `dictAdd()` และ `dictLookup()`
  - *แนวทาง*: ใส่ฟังก์ชัน `.trim()` ให้กับ Key ก่อนนำไปตรวจสอบเปรียบเทียบในอาร์เรย์

---

## 💻 4. หน้า `day0.html` (Introduction to Programming)

- [x] **[UI/UX] กำหนดค่า Default อัตโนมัติป้องกัน Error เมื่อผู้เรียนกรอกค่าว่างใน Variable Playground**
  - *ปัญหา*: เมื่อเลือกประเภทข้อมูลเป็น `int` แต่ไม่ได้กรอกค่าใด ๆ ลงในอินพุต ระบบจะนำสตริงเปล่าไปตรวจสอบกับ Regex ซึ่งจะแจ้ง Error สื่อสารไม่ตรงจุด
  - *จุดแก้ไข*: ฟังก์ชัน `createVar()` ในสคริปต์ท้ายไฟล์
  - *แนวทาง*: หากผู้เรียนคลิกสร้างโดยปล่อยช่องใส่ค่าว่างไว้ ให้ระบบสุ่มหรือนำค่า default ที่เหมาะสมมาแทนที่ให้โดยอัตโนมัติ (เช่น `0` สำหรับ `int`, `0.0` สำหรับ `float`, `""` สำหรับ `str`)

---

## 🏠 5. หน้า `index.html` (Main Menu Portal)

- [x] **[UI/UX] เพิ่มมิติความพรีเมียมให้หน้าโฮมพอร์ทัล**
  - *ปัญหา*: หน้าเมนูหลักเรียบง่ายเกินไป การ Hover ขาดแอนิเมชันที่ให้ความรู้สึกพรีเมียมและไหลลื่น
  - *จุดแก้ไข*: สไตล์ CSS ของ `.card` และ `.card-footer` ใน `index.html`
  - *แนวทาง*: เพิ่มเอฟเฟกต์การยกรอยเล็กน้อยพร้อมปรับสีแบบ Smooth Transition ใน CSS เพื่อให้ความรู้สึกน่าดึงดูดใจก่อนเริ่มเรียน



