# รายงานการตรวจสอบความผิดปกติของ day4.html

จากการตรวจสอบโครงสร้างและโค้ดใน [day4.html](file:///c:/Users/noey/Desktop/coop-dga/day4.html) พบจุดที่ผิดปกติและควรปรับปรุงทั้งหมด 4 จุดหลักดังนี้ครับ:

---

## 1. บั๊กการแสดงผลเส้นใต้ของแท็บเมนู (Tab Navigation Underline Styling Bug)
* **จุดที่เกิดปัญหา**: บรรทัดที่ 57-75 ในส่วนโครงสร้างแท็บ `<nav>` และบรรทัดที่ 381-398 ในฟังก์ชัน `openTab()`
* **อาการผิดปกติ**: 
  - แท็บแรก (`ml-basics`) มีคลาส `border-b-2` ติดมาตั้งแต่แรกใน HTML แต่แท็บอื่น ๆ ไม่มีคลาสนี้
  - ในฟังก์ชัน `openTab()` มีการลบและเพิ่มคลาส `border-blue-600` แต่**ไม่มีการจัดการคลาส `border-b-2`**
  - ส่งผลให้เวลาสลับไปแท็บอื่น เส้นใต้สีน้ำเงินหนา 2px จะไม่แสดงขึ้นมา และแท็บแรกจะยังมีพื้นที่เส้นใต้ค้างอยู่ ทำให้หน้าตาเมนูบิดเบี้ยวไม่สม่ำเสมอ
* **แนวทางแก้ไข**: เติมคลาส `border-b-2 border-transparent` ให้กับปุ่มแท็บทุกปุ่มใน HTML เพื่อจองพื้นที่เส้นใต้ไว้เท่ากัน และเขียน JS ให้เพิ่ม/ลบคลาส `border-transparent` สลับกับ `border-blue-600` เมื่อมีการคลิกเปลี่ยนแท็บ

---

## 2. จุดสเกลของกราฟ Logistic Regression ไม่ตรงกับแนวแกน Y (Sigmoid Curve Scaling Mismatch)
* **จุดที่เกิดปัญหา**: ฟังก์ชัน `draw()` ภายใน `initClassification()` (บรรทัดที่ 520-535)
* **อาการผิดปกติ**:
  - แกน Y ในกราฟ Canvas วาดส่วนสูงตั้งแต่พิกัด `y = 40` (ด้านบน: 1.0 หรือ 100%) ถึง `y = 340` (ด้านล่าง: 0.0 หรือ 0%) ซึ่งคิดเป็นส่วนสูงรวม **300 พิกเซล**
  - แต่การคำนวณตำแหน่งเส้นโค้ง Sigmoid และเส้นแบ่งเขตตัดสินใจ (Threshold Line) กลับคูณด้วยตัวเลขสเกล **260** เท่านั้น (`340 - prob * 260`)
  - ทำให้โอกาส 100% ของกราฟไปหยุดอยู่แค่พิกัด `y = 80` ส่งผลให้เส้นกราฟลอยไม่ถึงจุดสูงสุดของแกน Y (มีช่องว่างโล่งด้านบน) และเส้นเกณฑ์ 0.5 (50%) ไม่อยู่กึ่งกลางของแกน Y จริง ๆ (แกนกึ่งกลางคือ 190 แต่เส้นแสดงผลลอยต่ำไปอยู่ที่ 210)
* **แนวทางแก้ไข**: ปรับสูตรคำนวณตำแหน่ง `cy` ของกราฟ และ `threshY` ของเส้นเกณฑ์จากตัวคูณ `260` เป็น `300` เพื่อให้ความสูงสอดคล้องกับแกน Y บน Canvas พอดี

---

## 3. คำสะกดผิดภาษาไทย (Typos)
* **จุดที่เกิดปัญหา**: ตัวแปรข้อมูล `metricData` แท็บมาตรวัด Regression ในตัวแปรย่อย `R2` (บรรทัดที่ 600)
* **คำที่ผิด**:
  - คำว่า `"พฤทีความผันผวน"` ซึ่งสะกดผิดจากคำว่า **`"พฤติกรรมและความผันผวน"`** (เหมือนที่ใช้ในส่วนอื่น ๆ ของไฟล์)
  - คำว่า `"ดั่งเดิม"` ซึ่งสะกดผิดจากคำว่า **`"ดั้งเดิม"`**
* **คำอธิบายเดิม**: `...ช่วยอธิบายพฤทีความผันผวนของระบบได้ดีกว่าโมเดลค่าเฉลี่ยฐานดั่งเดิมเท่าไหร่`

---

## 4. มีการเรียกใช้คลาส CSS ที่ไม่ได้นิยามไว้ (Unused Class)
* **จุดที่เกิดปัญหา**: กล่องแสดงผล F1-Score (บรรทัดที่ 356)
* **รายละเอียด**: มีการใช้คลาส `border-gradient` ใน HTML `<div class="... border-gradient ... border-purple-500">` แต่เมื่อค้นหาในแท็ก `<style>` ด้านบน กลับไม่มีสไตล์ที่กำหนดไว้สำหรับคลาสนี้ (แม้ว่าจะไม่มีผลกระทบต่อสเปกหลักเนื่องจากมีคลาส `border-purple-500` คลุมไว้อยู่แล้ว แต่เป็นโค้ดขยะที่หลงเหลือไว้)
